//this is also a normal heap question here we will be given with a vector of pairs where each pair indicates the distacnce from the origin our aim is to return top k closest pairs to origin ot do so we need to create a max heap<int,pair<int,int> > ...