#include<bits/stdc++.h>
using namespace std;

int main()
{
	//xor of same bits is 0 |||ly xor of same numbers are also 0 as both will have same bits we can use this property for various questions
	//xor of any number with itself is 0 and any number with 0 is the same number

	//1.swap

	// int a=3,b=4;
	// a=a^b;
	// b=a^b;
	// a=a^b;

	// cout<<a<<" "<<b<<endl;

	//2.given with an array of integers which contains all the number in even count but except one find that one;

	// int ar[]={1,1,2,3,4,5,2,4,5,5,5};
	// int sum=0;
	// for(int i=0;i<11;i++)
	// 	sum^=ar[i];
	// cout<<sum;
	return 0;
}